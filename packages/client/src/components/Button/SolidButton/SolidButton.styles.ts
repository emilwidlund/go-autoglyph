import {StyleSheet} from 'react-native';

export const baseStyles = StyleSheet.create({
    container: {
        borderRadius: 14,
        backgroundColor: '#2222ff'
    },
    text: {
        color: '#fff'
    }
});
